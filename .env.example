# GeoWeld Environment Configuration
# Copy this file to .env and update with your values

# Mapbox API token for map visualization (Required)
# Get your token at: https://account.mapbox.com/access-tokens/
MAPBOX_TOKEN=your_mapbox_token_here

# Web Server Configuration
PORT=4011
NODE_ENV=production

# Optional: Adjust memory limits for processing large datasets
# Uncomment and adjust as needed for heavy processing workloads
# NODE_OPTIONS=--max-old-space-size=4096